export const environment = {
  /**
   * Application environment configuration for development builds.
   * apiBaseUrl is the base URL for backend API requests.
   */
  production: false,
  apiBaseUrl: 'http://localhost:3001'
};
