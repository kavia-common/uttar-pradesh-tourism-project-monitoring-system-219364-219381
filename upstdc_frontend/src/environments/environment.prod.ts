export const environment = {
  /**
   * Application environment configuration for production builds.
   * apiBaseUrl is the base URL for backend API requests.
   * If a different production API endpoint is needed, update this value accordingly.
   */
  production: true,
  apiBaseUrl: 'http://localhost:3001'
};
